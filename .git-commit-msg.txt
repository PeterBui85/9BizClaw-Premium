fix: conversation.js setMemoryWriteNotifyCeo wire + customer-memory audit fix

- Add setMemoryWriteNotifyCeo to conversation.js: wires CEO Telegram
  alerts into customer memory writes (main.js calls setMemoryWriteNotifyCeo
  with sendMemoryWriteAlert so CEO gets notified when bot writes memory).
- Fix customer-memory/write: replace non-existent appendMemoryWriteAudit
  (conversation.js) with auditLog (workspace.js).
