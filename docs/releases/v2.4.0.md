# 9BizClaw v2.4.0 — Google Calendar integration (CEO-owned OAuth)

Ngày: (TBD) · Bản lớn — thay thế feature "Lịch hẹn" local bằng Google Calendar two-way sync

## Nổi bật

**Kết nối Google Calendar** — sếp xem + quản lý lịch hẹn trong Dashboard, bot tạo/sửa/xóa lịch qua Telegram chat. Dùng Google Calendar riêng của sếp, không có MODORO trung gian.

## Cách thiết lập

Vào tab "Lịch hẹn" → "Kết nối Google Calendar" → wizard 6 bước (~15 phút một lần duy nhất):

1. Tạo Google Cloud project (deep-link trong wizard)
2. Bật Calendar API
3. Cấu hình OAuth consent — **bấm PUBLISH APP** để refresh token bền
4. Tạo OAuth Client ID + redirect URI `http://127.0.0.1:20199/gcal/callback`
5. Dán Client ID + Secret vào 9BizClaw, bấm Kiểm tra (validate qua Google token endpoint)
6. Đăng nhập Google — xong

## Dùng qua Telegram (bot CRUD markers)

```
Sếp: thêm lịch họp Huy ngày mai 2pm 30 phút
Bot: Đã tạo lịch 20/04 14:00 'Họp Huy' · link: calendar.google.com/...

Sếp: lịch tuần này?
Bot: Tuần này có 8 lịch: Thứ 2 09:00 Họp team • ...

Sếp: dời cuộc Huy sang 4pm
Bot: Dạ có cuộc với Huy 22/04 14:00. Sếp xác nhận dời sang 16:00?
Sếp: ừ
Bot: Đã dời 'Tư vấn Huy' sang 22/04 16:00-17:00.

Sếp: thứ 5 có rảnh không
Bot: Sếp bận 2 khoảng: 09:00-10:00, 14:00-15:30.

Sếp: xóa cuộc Huy
Bot: Xác nhận xóa 'Tư vấn Huy' 22/04 16:00?
Sếp: ok
Bot: Đã xóa lịch.
```

5 markers hoạt động sau scene: `[[GCAL_CREATE]]`, `[[GCAL_LIST]]`, `[[GCAL_UPDATE]]`, `[[GCAL_DELETE]]`, `[[GCAL_FREEBUSY]]`. Bot tự parse tiếng Việt → ISO trước khi emit. DELETE + UPDATE yêu cầu xác nhận CEO turn trước (10 phút HOẶC 5 turn, tính cái ngắn hơn).

## Dashboard UI

- Tab "Lịch hẹn" — 2 state: chưa kết nối (CTA + wizard) / đã kết nối (event list 2-column + detail pane)
- Create/Edit modal — tiêu đề + bắt đầu + thời lượng + guests + địa điểm + mô tả
- Settings submodal — calendar dropdown, giờ làm, slot duration, reminder minutes

## Migration từ v2.3.48

- Lịch hẹn local cũ tự động xuất sang `.learnings/appointments-archive-<date>.md` trên boot đầu sau upgrade
- `appointments.json` → rename thành `appointments.json.bak` (giữ raw data)
- Cron reminder cũ đọc file local bị gỡ — Google Calendar mobile app lo notification
- **Không rollback được ở data layer.** Downgrade về v2.3.48: archive .md vẫn ở `.learnings/` nhưng app không còn feature cũ. Archive là bản ghi duy nhất — CEO re-enter thủ công vào Google Calendar nếu cần.

## Bảo mật

- CLIENT_ID + CLIENT_SECRET của sếp stored qua `electron.safeStorage` (Mac Keychain / Windows DPAPI / Linux libsecret). Linux không keyring → fallback plain `0600` + boot warning hiện trong Dashboard.
- Refresh token + access token stored tại `~/.openclaw/gcal-tokens.json` (safeStorage-encrypted payload khi keyring sẵn sàng, plaintext 0600 fallback khi không).
- Disconnect revoke token server-side qua `oauth2.googleapis.com/revoke` trước khi xóa local — dù credential có leak qua backup thì token đã dead ở server.
- Audit log tại `logs/audit.jsonl` (shared audit log — filter bằng `jq '.event | startswith("gcal")'` để xem riêng event gcal) — NEVER logs `access_token` / `refresh_token` / `client_secret` (allowlist recursive, test fixtures cover `ya29.` / `1//` / `GOCSPX-` prefix smuggling qua nested `patch` objects).
- Marker injection defense: customer typing `[[GCAL_DELETE]]` trong Zalo DM bị neutralize thành `[GCAL-blocked-DELETE]` trước khi bot thấy. Patch anchor verified multi-line `if (!rawBody && !hasMedia) { return; }` — anchor-miss FAIL LOUD (spec: input-side defense load-bearing, không silent-skip).
- 412 ETag: update-event có max 2 PATCH attempts (1 retry sau fetch fresh etag). Last-write-wins, không optimistic concurrency v1.
- 429/403 userRateLimitExceeded: surface Vietnamese "Google đang giới hạn — thử lại sau vài phút" thay vì raw HTTP status.
- 401 refresh revoked: surface "Kết nối Google hết hạn — vào Lịch hẹn → Ngắt kết nối → Kết nối lại".

## Kỹ thuật

| | |
|---|---|
| Tasks shipped | 28 (5 chunk 1 foundation + 10 chunk 2 IPC/API + 13 chunk 3 markers/UI/polish) |
| Commits | 30+ atomic commits on main (28 original tasks + 3 post-ship deep-review batches) |
| Files touched | `electron/main.js` (+~800 lines), `electron/gcal/*` (6 files: auth, calendar, config, credentials, markers, migrate), `electron/ui/dashboard.html` (+~500 lines 4 modals), `electron/preload.js`, `electron/scripts/smoke-gcal.js` (8 tests), `AGENTS.md` (v48→v49), `docs/releases/v2.4.0.md` |
| Smoke suites | 5/5 pass after every commit (test, context-injection, zalo-followup, visibility, gcal) |
| Gcal smoke tests | 8 (credentials, config, migration, marker parse, date shape, audit token exclusion, neutralize, scrub) |
| AGENTS.md version | 48 → 49 (auto-backup to `.learnings/AGENTS-backup-v48-*.md` on upgrade) |
| New workspace files | `gcal-credentials.enc` (or `.plain`), `gcal-tokens.json`, `gcal-config.json`, `.learnings/appointments-archive-*.md`, `.learnings/appointments-migrated.flag` (gcal events share `logs/audit.jsonl`) |
| HTTP error classification | QUOTA / UNAUTHORIZED / NOT_FOUND / ETAG_MISMATCH — all 5 gcal IPC handlers translate to VN |

## Hoãn sang v2.5.0

- Customer-facing Zalo auto-booking (bot đặt lịch trực tiếp cho khách qua Zalo DM — cần threat model + conversational disambiguation + double-book guard)
- Multi-calendar support (work + personal)
- Recurring events (RRULE)
- Google Meet link auto-create
- Proactive Telegram reminders (Google Calendar mobile app đã làm đủ tốt)
- Staff calendar sharing

Internal build — dev test trên máy sếp trước khi ship rộng.

## Hotfixes from extreme deep review (post-28-task)

Sau 28 task ship ban đầu, 5 reviewer song song tìm ra 7 P0 + 14 P1 issues chưa ai bắt được. Tổng fix thêm 3 batch:

**Batch 1 (9 fixes)** — commit c3c88db:
- sendZalo + sendTelegramRich nay chạy interceptGcalMarkers (lỗ leak raw JSON sang Zalo customers)
- ensureZaloGcalNeutralizeFix bọc try/catch + CEO alert (upstream openzalo đổi anchor sẽ không crash boot)
- OAuth state param chống CSRF / token fixation
- audit log error field scrub ya29./1///GOCSPX- token prefix
- sendCeoAlert (skipFilter) path nay neutralize marker không thực thi (tránh phantom event từ stderr)
- getAccessToken in-flight mutex (tránh 2 concurrent refresh)
- OAuth port 20199 EADDRINUSE Vietnamese actionable error
- Disconnect giữ credentials (chỉ xóa tokens) — reconnect không cần GCP redo

**Batch 2 (4 fixes)** — commit a3894ca:
- Wizard validation gates — Step 5 không next được nếu chưa validate, Step 6 không Hoàn tất được nếu chưa OAuth
- Edit event nay giữ + cập nhật được guest list (calendar.js project attendees, updateEvent pass through)
- OAuth failure UI có Retry + Back-to-Step-5 button + support link
- Marker-path errors translate sang Vietnamese (QUOTA/UNAUTHORIZED/NOT_FOUND/network)

**Batch 3 (4 fixes)** — commit (this):
- electron/package.json version 2.3.48 → 2.4.0 (EXE self-report giờ match release)
- AGENTS.md contradiction fix (xóa §Quản lý lịch hẹn cho CEO stale + delete skills/appointments.md + INDEX.md row)
- README.md version + feature list update (remove CRUD lịch hẹn local)
- Release note audit log path + commit count + new CLAUDE.md patch entry

Smoke 5/5 PASS xuyên suốt.
